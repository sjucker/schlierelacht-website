# Mapbox Access Token
# Get it from https://account.mapbox.com/
MAPBOX_API_TOKEN=

# Backend API base URL. If unset, falls back to https://schlierelacht-api.ch.
# For local development point it at the Spring Boot app:
# API_BASE_URL=http://localhost:8080
API_BASE_URL=
